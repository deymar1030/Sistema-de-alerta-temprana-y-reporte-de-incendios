// Placeholder para la capa de notificaciones en tiempo real.
// Cuando se agregue socket.io u otra librería, la inicialización va aquí.
export function initNotificationSocket(httpServer) {
  void httpServer;
  console.log("Notification socket: pendiente de implementar");
}
